import { Canvas, Meta } from '@storybook/addon-docs'
import { Colors, colorList } from './Color'

<Meta title="Basics/Colors" />

# Colors

The following colors are available for usage in the app, via CSS custom properties.

<Colors colorList={colorList} />
